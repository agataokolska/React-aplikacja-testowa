import React from 'react'

const SingingButton = (props) => (
    <div>
        <button
            onClick={()=> alert ('lalala')}
        >
            Label
        </button>
    </div>
)

export default SingingButton